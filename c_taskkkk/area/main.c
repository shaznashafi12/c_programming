#include <stdio.h>

int main()
{
    int l,b,area;
    printf("enter the length");
    scanf("%d",&l);
    printf("enter the breadth");
    scanf("%d",&b);
    area=l*b;
    printf("area=%d",area);
    return 0;
}
